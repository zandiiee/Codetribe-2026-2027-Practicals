try{
    console.log(divide(10, 0));
    let num = 3/0;
    console.log(num.toUpperCase());            
} catch(error){
    console.log("An error occured:", error.message);
}

let user = {
    name:" Zandie",
    age:23
}